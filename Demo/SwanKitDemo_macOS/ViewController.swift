
import Cocoa
import SwanKit

class ViewController: NSViewController {
    override func loadView() {
        view = NSView()
        view.frame = NSRect(x: 0, y: 0, width: 800, height: 600)
    }

    override func viewDidLoad() {
        super.viewDidLoad()

        // Пример использования фреймворка для macOS
    }
}
